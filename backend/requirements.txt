# CommerceOS / Nexus — backend runtime dependencies
# Pinned to the versions validated on Python 3.11. Update via `pip-compile` or deliberate bumps.

# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.52.1
gunicorn==23.0.0
python-multipart==0.0.20
sse-starlette==2.1.3

# --- Config & validation ---
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.1
email-validator==2.2.0

# --- Database ---
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
alembic==1.14.0

# --- Cache / realtime ---
redis==8.1.0
websockets==16.1.1

# --- Data / stats ---
pandas==2.3.3
numpy==2.3.5

# --- AI / agents (LangChain-native) ---
langchain==1.4.0
langchain-core==1.6.2
langgraph==1.2.11
langgraph-checkpoint==4.2.0
langgraph-prebuilt==1.1.0
langgraph-checkpoint-postgres==3.1.2
langchain-groq==1.1.3
langchain-openai==1.6.2
# langchain-anthropic / langchain-aws — install when using those providers
openai==3.11.0
tiktoken==0.8.0
sqlparse==0.5.3

# --- Auth / security ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0

# --- Resilience ---
tenacity==9.1.4

# --- Observability ---
structlog==26.1.0
prometheus-client==0.26.0
prometheus-fastapi-instrumentator==7.0.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-sqlalchemy==0.50b0
opentelemetry-exporter-otlp-proto-http==1.29.0

# --- AWS ---
boto3==1.35.99

# --- HTTP client ---
httpx==0.28.1
